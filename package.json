{
  "name": "challenge-module-generate-pi",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rm -rf ./dist && tsc -p tsconfig.json",
    "start": "node dist",
    "start:dev": "nodemon --watch src --exec ts-node src/index.ts",
    "test": "jest --passWithNoTests"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zivkong/challenge-module-generate-pi.git"
  },
  "author": "Ziv Kong",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/zivkong/challenge-module-generate-pi/issues"
  },
  "homepage": "https://github.com/zivkong/challenge-module-generate-pi#readme",
  "dependencies": {
    "dotenv": "^16.0.1",
    "fastify": "^4.2.0",
    "mongoose": "^6.4.4"
  },
  "devDependencies": {
    "@types/jest": "^28.1.4",
    "@types/node": "^18.0.3",
    "jest": "^28.1.2",
    "ts-jest": "^28.0.5",
    "ts-node": "^10.8.2",
    "typescript": "^4.7.4"
  },
  "jest": {
    "rootDir": "src",
    "preset": "ts-jest",
    "testEnvironment": "node",
    "transform": {
      "node_modules/variables/.+\\.(j|t)sx?$": "ts-jest"
    },
    "transformIgnorePatterns": [
      "node_modules/(?!variables/.*)"
    ]
  }
}
